<h1>Pesquisa realizada pelo filtro</h1>
<hr>
<main>
    <div class="container">
        <section class="container">
            <table class="table table-hover myTable">
                <thead>
                    <tr>
                        <th>Nome</th>
                        <th>Cidade</th>
                        <th>Endereço</th>
                        <th>Telefone</th>
                        <th>Data nascimento</th>
                        <th>E-mail</th>
                    </tr>
                </thead>
                <tbody class="container">
                    {{#each dados}}
                    <tr>
                        <td><span>
                                <ion-icon name="person"></ion-icon>
                            </span> {{nm_cliente}}</td>
                        <td>{{nm_cidade}}</td>
                        <td>{{nm_endereco}}</td>
                        <td>{{nm_telefone}}</td>
                        <td>{{dt_nascimento}}</td>
                        <td>{{nm_email}}</td>
                        <td><a
                                href="selecionar-cliente/{{nm_cliente}}/{{nr_sequencia}}/{{nm_telefone}}/{{nm_endereco}}/{{dt_nascimento}}">
                                <button class="btn btn-success"><span>
                                        <ion-icon name="information-circle"></ion-icon>
                                    </span> Selecionar</button></a>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            <hr>
            <strong><span class="totalCliente">Total de clientes {{total_agenda}}</span></strong>
            <a href="/form-agenda/cad-agenda" class="xx"><button id="link" class="btn btn-secondary"><span>
                        <ion-icon name="funnel"></ion-icon>
                    </span>Reset</button></a>
        </section>
    </div>
</main>